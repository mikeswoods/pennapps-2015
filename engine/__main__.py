import engine
import engine.mosaic

################################################################################

engine.mosaic.create(engine.TEST_IMAGES[3], k=5, start_window=160, n=6)

################################################################################
